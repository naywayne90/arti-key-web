import { Box, Typography } from '@mui/material';

export default function Formations() {
  return (
    <Box>
      <Typography variant="h4">Gestion des Formations</Typography>
      {/* Contenu à venir */}
    </Box>
  );
}
